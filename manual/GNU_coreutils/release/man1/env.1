.\" DO NOT MODIFY THIS FILE!  It was generated by help2man 1.50.1.
.\" Updated 2025-11-13, ribbon,GNU coreutils 9.8
.\"
.mso ja.tmac
.hla ja
.TH ENV "1" "2025/09" "GNU coreutils 9.8" "ユーザーコマンド"
.SH 名前
env \- 変更した環境でプログラムを実行する
.SH 書式
.B env
[\fI\,OPTION\/\fR]... [\fI\,-\/\fR] [\fI\,NAME=VALUE\/\fR]... [\fI\,COMMAND \/\fR[\fI\,ARG\/\fR]...]
.SH 説明
.\" Add any additional description here
.PP
環境変数 NAME を VALUE に設定し、 COMMAND を実行します。
.PP
長いオプションで必須となっている引数は短いオプションでも必須です。
.TP
\fB\-a\fR, \fB\-\-argv0\fR=\fI\,ARG\/\fR
COMMAND に ARG を 0 番目の引数として渡します。
.TP
\fB\-i\fR, \fB\-\-ignore\-environment\fR
環境変数がない状態で開始します。
.TP
\fB\-0\fR, \fB\-\-null\fR
出力行の区切りとして改行文字ではなく NUL を使用します。
.TP
\fB\-u\fR, \fB\-\-unset\fR=\fI\,NAME\/\fR
環境変数から指定した変数を削除します。
.TP
\fB\-C\fR, \fB\-\-chdir\fR=\fI\,DIR\/\fR
作業ディレクトリを DIR に変更します。
.TP
\fB\-S\fR, \fB\-\-split\-string\fR=\fI\,S\/\fR
S を個々の引数に分割します。
shebang 行で複数の引数を渡すのに使用されます。
.TP
\fB\-\-block\-signal\fR[=\fI\,SIG\/\fR]
シグナル SIG の COMMAND への配送をブロックします。
.TP
\fB\-\-default\-signal\fR[=\fI\,SIG\/\fR]
シグナル SIG の処理をデフォルトにリセットします。
.TP
\fB\-\-ignore\-signal\fR[=\fI\,SIG\/\fR]
シグナル SIG の処理を何もしないに設定します。
.TP
\fB\-\-list\-signal\-handling\fR
シグナルの処理がデフォルトと異なるものを標準エラーに表示します。
.TP
\fB\-v\fR, \fB\-\-verbose\fR
処理ステップ毎に詳しい情報を表示します。
.TP
\fB\-\-help\fR
使い方を表示して終了します。
.TP
\fB\-\-version\fR
バージョン情報を表示して終了します。
.PP
\- だけを指定した場合は \fB\-i\fR を指定したことになります。
COMMAND が指定されなければ最終的な環境変数を表示します。
.PP
SIG には、 'PIPE' のようなシグナル名、 '13' のようなシグナル番号指定できます。
SIG が指定されなかった場合、既知のすべてのシグナルが対象となります。
複数のシグナルをコンマ区切りで指定できます。引数のない SIG は何もしません。
.SS "終了ステータス:"
.TP
125
env コマンド自身が失敗
.TP
126
COMMAND が見つかったが、起動できない
.TP
127
COMMAND が見つからない
.TP
\-
COMMAND の終了ステータスなど
.SH "スクリプトオプションの処理"
.B \-S
オプションを使うと、スクリプト中で複数の引数を指定できます。
.B 1.pl
という名前のスクリプトの最初の行に以下が含まれているとします。
.PP
.RS
.nf
#!/usr/bin/env \-S perl \-w \-T
\&...
.fi
.RE
.PP
これは以下が実行されます。
.B "perl \-w \-T 1.pl"
.PP
.B '\-S'
パラメータを指定しない場合、おそらく以下のような感じでスクリプトは失敗するでしょう。
.PP
.RS
.nf
/usr/bin/env: 'perl \-w \-T': No such file or directory
.fi
.RE
.PP
詳細については完全なドキュメントを参照してください。
.SH 注意
POSIX の exec(3p) のページには以下のような記載があります。
.RS
「多くのアプリケーションは、
特定のシグナルがデフォルト動作に設定されていたりブロックされていない状態で
アプリケーションが開始される、という誤った前提を置いている。。。。
そのため、最も良いのは、明確な理由がない限りは exec の前後でシグナルを
ブロックしたり無視したりしないことだ。
特に、(密接に連携しているわけではない) 任意のプログラム間の exec では
シグナルをブロックしないのがよい。」
.RE
.SH 作者
作者 Richard Mlynarik、 David MacKenzie、および Assaf Gordon。
.SH バグのレポートについて
バグのレポートは bug\-coreutils@gnu.org までお寄せ下さい。
.br
GNU coreutils のホームページ: <https://www.gnu.org/software/coreutils/>
.br
一般的な GNU  ソフトウェア使用に関するヘルプは <https://www.gnu.org/gethelp/>
です。
.br
翻訳に関するバグは <https://translationproject.org/team/ja.html> に連絡してく
ださい。
.SH 著作権
Copyright \(co 2025 Free Software Foundation, Inc.
ライセンス GPLv3+: GNU GPL version 3 or later <https://gnu.org/licenses/gpl.html>.
.br
This is free software: you are free to change and redistribute it.
There is NO WARRANTY, to the extent permitted by law.
.SH 関連項目
\fBsigaction\fP(2), \fBsigprocmask\fP(2), \fBsignal\fP(7)
.PP
.br
完全なマニュアルは <https://www.gnu.org/software/env> か、
.br
ローカルで有効になっていれば info \(aq(coreutils) env invocation\(aq です。
.SH 日本語訳
対応バージョン:9.8
翻訳日:2025-11-13
翻訳者:ribbon
