@c This file contains the entry for the -w/--debug-dump (readelf) and
@c  -W/--dwarf (objdump) option that is common to both readelf and objdump.

ファイル内の DWARF デバッグセクションの内容を表示します (存在する場合)。圧縮されたデバッグセクションは、表示される前に自動的に (一時的に) 解凍されます。スイッチの後にオプションの文字または単語が 1つ以上続く場合は、それらのタイプのデータのみがダンプされます。文字と単語は以下の情報を参照します:

@c Please Keep This Table Alpha Sorted.
@table @code
@item a
@itemx =abbrev
@samp{.debug_abbrev} セクションの内容を表示します。

@item A
@itemx =addr
@samp{.debug_addr} セクションの内容を表示します。

@item c
@itemx =cu_index
@samp{.debug_cu_index} セクションと @samp{.debug_tu_index} セクションの内容を表示します。

@item f
@itemx =frames
@samp{.debug_frame} セクションの内容をそのまま表示します。

@item F
@item =frames-interp
@samp{.debug_frame} セクションの解釈された内容を表示します。

@item g
@itemx =gdb_index
@samp{.gdb_index} セクションと @samp{.debug_names} セクションの内容を表示します。

@item i
@itemx =info
@samp{.debug_info} セクションの内容を表示します。注意: このオプションからの出力は、 @option{--dwarf-depth} および @option{--dwarf-start} オプションを使用して制限することもできます。

@item k
@itemx =links
@samp{.gnu_debuglink}、 @samp{.gnu_debugaltlink}、および @samp{.debug_sup} セクションが存在する場合、その内容を表示します。また、 @samp{.debug_info} セクション の DW_AT_GNU_dwo_name または DW_AT_dwo_name 属性によって指定されている場合は、個別の dwarf オブジェクトファイル (dwo) へのリンクも表示します。

@item K
@itemx =follow-links
リンクされた個別のデバッグ情報ファイルで見つかった、選択されたデバッグセクションの内容を表示します。これにより、同じデバッグセクションが複数のファイルに存在する場合、複数のバージョンが表示される可能性があります。

さらに、 DWARF 属性を表示するときに、個別のデバッグ情報ファイルを参照するフォームが見つかった場合は、参照された内容も表示されます。

注意 - 一部のディストリビューションでは、このオプションはデフォルトで有効になっています。@option{N} デバッグオプションを使用して無効にできます。デフォルトは、 binutils の設定時に @option{--enable-follow-debug-links=yes} または @option{--enable-follow-debug-links=no} オプションで選択できます。これらが使用されない場合、デフォルトでは次のデバッグリンクが有効になります。

注意 - binutils が構築された時に debuginfod プロトコルのサポートが有効になっていた場合、このオプションは @var{DEBUGINFOD_URLS} 環境変数に指定された debuginfod サーバへの接続も行います。この問題の解決には時間がかかる場合があります。この動作は、デバッグオプション @option{=do-not-use-debuginfod} を使用して無効にできます。

@item N
@itemx =no-follow-links
個別のデバッグ情報ファイルへの次のリンクを無効にします。

@item D
@itemx =use-debuginfod
デバッグリンクをたどる必要がある場合に、 debuginfod サーバとの通信を有効にします。これがデフォルトの動作です。

@item E
@itemx =do-not-use-debuginfod
デバッグリンクをたどる必要がある場合に、debuginfod サーバへの接続を無効にします。

@item l
@itemx =rawline
@samp{.debug_line} セクションの内容を raw 形式で表示します。

@item L
@item =decodedline
@samp{.debug_line} セクションの解釈された内容を表示します。

@item m
@itemx =macro
@samp{.debug_macro} セクションと @samp{.debug_macinfo} セクションの内容を表示します。

@item o
@itemx =loc
@samp{.debug_loc} セクションおよび @samp{.debug_loclists} セクションの内容を表示します。

@item O
@itemx =str-offsets
@samp{.debug_str_offsets} セクションの内容を表示します。

@item p
@itemx =pubnames
@samp{.debug_pubnames} および @samp{.debug_gnu_pubnames} セクションの内容を表示します。

@item r
@itemx =aranges
@samp{.debug_aranges} セクションの内容を表示します。

@item R
@itemx =Ranges
@samp{.debug_ranges} セクションと @samp{.debug_rnglists} セクションの内容を表示します。

@item s
@itemx =str
@samp{.debug_str}、@samp{.debug_line_str} 、@samp{.debug_str_offsets} の各セクションの内容を表示します。

@item t
@itemx =pubtype
@samp{.debug_pubtypes} および @samp{.debug_gnu_pubtypes} セクションの内容を表示します。

@item T
@itemx =trace_aranges
@samp{.trace_aranges} セクションの内容を表示します。

@item u
@itemx =trace_abbrev
@samp{.trace_abbrev} セクションの内容を表示します。

@item U
@itemx =trace_info
@samp{.trace_info} セクションの内容を表示します。

@end table

注意: @samp{.debug_static_funcs}、@samp{.debug_static_vars} 、および @samp{debug_weaknames} セクションの内容の表示は、現在サポートされていません。

@item --dwarf-depth=@var{n}
@code{.debug_info} セクションのダンプを @var{n} の子に制限します。
これは @option{--debug-dump=info} を指定した場合にのみ有用です。デフォルトでは、すべての DIE が出力されます。@var{n} の特別な値 0 もこの効果を持ちます。

@var{n} にゼロ以外の値を指定すると、 @var{n} 位以上の DIE は出力されません。@var{n} の範囲は 0 ベースです。

@item --dwarf-start=@var{n}
番号 @var{n} の DIE で始まる DIE だけを出力します。これは @option{--debug-dump=info} を指定した場合にのみ有用です。

このオプションを指定すると、番号が @var{n} の DIE より前のすべてのヘッダー情報およびすべての DIE の表示が抑制されます。指定された DIE の兄弟と子だけが出力されます。

これは @option{--dwarf-depth} と組み合わせて使用できます。

